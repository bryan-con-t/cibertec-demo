<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Marcado -->
    <item android:drawable="@drawable/ic_checked" android:state_checked="true" android:color="@color/azul2" />
    <!-- No marcado -->
    <item android:drawable="@drawable/ic_unchecked" android:color="@color/morado6" />
</selector>